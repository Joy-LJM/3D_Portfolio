services:
  app:
    build: 
      context: .
      dockerfile: Dockerfile
    volumes:
    - .:/app 
    - /app/node_modules
    command: npm run dev -- --host 0.0.0.0
    environment:
      - NODE_ENV=development